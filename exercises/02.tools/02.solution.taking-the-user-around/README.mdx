# 🏁 The bot can drive

Your bot can now move the customer around the site straight from the chat. Same `toolDefinition(...)` you already knew — except this one runs in the *browser*, not on the server. The model can't tell the difference, and that symmetry is the whole point. 🎉

Well done. Take a breather before the next step.

## 👨‍💼 Peter has notes

> The navigation is slick! But customers are getting bolder — "add this to my cart", "just buy it for me." That makes me nervous. We cannot let an AI mutate someone's cart without a confirmation. The bot needs to ask permission before it acts.

Next step: a human-in-the-loop approval flow.

🧝‍♀️ Kellie built the `addToCart` plumbing ahead of you — the cart actions, the variations tool, and the little product-info endpoint the cart needs — so next step you can focus on the *approval gate* itself, not on building the whole feature.
