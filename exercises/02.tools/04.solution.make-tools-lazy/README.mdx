# 🏁 Lazy tools, big savings

Your bot now has eleven tools but only shows the model the handful it needs on each message — the rest sit behind a discovery tool and load on demand. All it took was `lazy: true`. And that wraps the whole **Tools** section: you've built server tools, client tools, approval flows, and lazy loading, and combined them into one assistant. 🎉

That was a big section — definitely earn a break before the next one.

## 👨‍💼 Peter has notes

> The assistant is fantastic at *doing* things now. But customers keep asking it to *compare* — "is the Nike or the Adidas a better deal?" — and it writes a wall of text that's impossible to scan. I want a proper side-by-side comparison we can render as a real table.

Next section: **structured replies** — getting typed, validated objects out of the model instead of prose.

🧝‍♀️ Kellie went big for this one: she built the entire compare experience ahead of you — the compare page and route, the comparison table and chat components, the compare context, and the supporting API routes. Next section you get to focus purely on the structured output: the schema and the wiring, not the UI.
