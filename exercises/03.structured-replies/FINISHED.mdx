# Section complete 🎉

You turned the model's reply from prose into **data**. The compare page no longer parses text — the chat hands `useChat` a typed comparison object and a React component renders it. Tools still work alongside `outputSchema`, so the model can look up real attributes before filling the schema in.

Let me know what you think in the form on the right! 👉
