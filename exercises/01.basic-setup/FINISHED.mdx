# Basic Setup

<h2>The chat panel is a focused EpicStore assistant! 🎉</h2>

You built the full path end to end: a server route that checks the input, a chat panel that uses `useChat`, and a bot with a clear role that knows which product the customer is looking at.

Please share your thoughts in the form on the right! 👉

Done? Let's go to the next section — we will give the bot tools so it can look up other products from the real catalog. 🚀
