# 🏁 The bot has a job now

You turned a generic chatbot into EpicStore's shopping assistant — it refuses off-topic questions, knows the exact product the customer is looking at, and even reasons before it answers. System prompts, `forwardedProps`, and `modelOptions` are the three dials behind that, and you just learned all of them. 🎉

Nice. Take a short break before the next one.

## 👨‍💼 Peter has notes

> It finally sounds like one of our people! But it's stuck — it only knows the one product on the screen. A customer asked "got anything by Nike under $200?" and it couldn't answer, because it has no way to actually look anything up in our catalog.

Next section: we give the bot **tools** — real functions it can call to reach into the live catalog and do things it simply can't do with words alone.
